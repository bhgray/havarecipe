# Only needed when running the frontend standalone (npm run dev) outside the Aspire AppHost.
# When launched via the AppHost, Aspire injects VITE_API_URL automatically with the live API port.
VITE_API_URL=https://localhost:7162
